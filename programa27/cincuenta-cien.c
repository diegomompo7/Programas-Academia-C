#include <stdio.h>
#include <stdlib.h>

int main(){

    for(int x=50; x<=100; x++){
	printf("%i - ", x);
    }

    /* while (x <= 100)
	{
	 printf("%i",x);
  	 printf(" - ");
	 x = x + 1;
	}
    */

    return EXIT_SUCCESS;
}
