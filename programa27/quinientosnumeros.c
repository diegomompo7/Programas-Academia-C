#include <stdio.h>
#include <stdlib.h>

int main(){

    for(int x=1; x<=500; x++){
	printf("%i - ", x);
    }

    /* while (x <= 500)
	{
	 printf("%i",x);
  	 printf(" - ");
	 x = x + 1;
	}
    */

    return EXIT_SUCCESS;
}
